name: kidsphonics
description: KidsPhonics - Mobile App for Grade 1 Learners
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  crypto: ^3.0.7               # Offline salted Parent PIN hashing
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2    # Progress saving
  google_fonts: ^6.1.0          # Nunito + Fredoka One fonts
  provider: ^6.1.1              # State management
  audioplayers: ^6.0.0          # SFX only (correct, wrong, flip, win beeps)
  flutter_tts: ^4.2.0           # Text-to-speech (en-US primary, en-PH fallback)
  speech_to_text: ^7.0.0        # Word recognition (not pronunciation scoring)
  permission_handler: ^11.3.1   # Microphone permission

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  http: ^1.2.2                    # used by tools/ generator scripts
  flutter_launcher_icons: ^0.14.3 # generates the Android app icon

flutter:
  uses-material-design: true
  fonts:
    - family: Nunito
      fonts:
        - asset: assets/fonts/Nunito-Regular.ttf
        - asset: assets/fonts/Nunito-Bold.ttf
          weight: 700
        - asset: assets/fonts/Nunito-ExtraBold.ttf
          weight: 800
        - asset: assets/fonts/Nunito-Black.ttf
          weight: 900
  assets:
    - assets/fonts/
    - assets/images/
    - assets/images/A-Z_Letter/
    - assets/images/Memory_Sound_Match_Quiz/
    - assets/images/Rhyming_Words/
    - assets/images/Word_Builder/
    - assets/audio/

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/images/app_icon.png"
  min_sdk_android: 21
  adaptive_icon_background: "#5636A6"
  adaptive_icon_foreground: "assets/images/app_mascot.png"
  adaptive_icon_foreground_inset: 22
